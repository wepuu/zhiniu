# event_engine

The Phase 7 event engine is implemented inside the modular API monolith at
`apps/api/src/zhaoniu_api/corporate_events`. This package boundary remains reserved for a future
extraction only after measured reuse or scale justifies it. See `docs/CORPORATE_EVENT_ENGINE.md`.
